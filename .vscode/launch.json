{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug regex_rustler",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/regex_rustler",
      "args": ["-f", "input.txt","-r","[a-z]+"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
